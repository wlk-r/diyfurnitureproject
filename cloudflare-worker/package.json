{
  "name": "pdf-watermark-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for watermarking furniture plan PDFs",
  "main": "pdf-watermark-worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
